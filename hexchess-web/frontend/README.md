# Frontend Notes

Vue2 battle screen:

- `MatchPanel.vue`: match state, API calls, side panels, move list.
- `GameBoard.vue`: 11x11 CSS hex board and cell click events.
- `src/api/matches.js`: REST client.

The dev server proxies `/api` to `http://localhost:8080`.
